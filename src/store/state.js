/**
 * Created by Wulongtao on 2017/7/14.
 */
import { asyncRouterMap, constantRouterMap } from 'src/router'

export const state = {
  routes: constantRouterMap,
  asyncRoutes: asyncRouterMap,
  clickedRoutes: [],
}
